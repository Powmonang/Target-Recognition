<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.defectscan.mapper.UserMapper">
    <insert id="addUser" parameterType="com.defectscan.entity.User">
        INSERT INTO users VALUES(#{username}, #{password}, #{userType}, #{name}, #{sex}, #{phone}, #{finalLogin})
    </insert>
    <update id="changeUser" parameterType="com.defectscan.entity.User">
        update users <set>
        <if test="password!=null">
            password = #{password},
        </if>
        <if test="phone!=null">
            phone = #{phone},
        </if>
        <if test="name!=null">
            name = #{name},
        </if>
        <if test="sex!=null">
            sex = #{sex},
        </if>
        <if test="finalLogin!=null">
            finalLogin = #{finalLogin}
        </if>
        </set>
        where username = #{username};
    </update>

    <select id="findUsers" parameterType="com.defectscan.entity.User" resultType="com.defectscan.entity.User">
        select * from users
        <where>
            <if test="username!=null">
                username = #{username}
            </if>
            <if test="password!=null">
                and password = #{password}
            </if>
        </where>
    </select>
</mapper>